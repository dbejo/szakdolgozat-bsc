% Margók beállítása
\usepackage[a4paper,inner=3cm,outer=2.5cm,top=2.5cm,bottom=2.5cm]{geometry}

% Betűtípus
\usepackage{times}

% Sortávolság beállítása
\usepackage{setspace}
\onehalfspacing  % 1.5 sortávolság
\frenchspacing
\sloppy

% Sorkizárt elrendezés
\usepackage{ragged2e}
\justifying

% Bekezdés behúzása
\setlength{\parindent}{0.75cm}
\usepackage{indentfirst}

% Elválasztás engedélyezése
\usepackage{t1enc}
\usepackage[hungarian]{babel}
